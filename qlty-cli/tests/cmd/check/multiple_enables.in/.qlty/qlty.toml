config_version = "0"

[plugins.definitions.multiple_enables]
file_types = ["shell"]

[plugins.definitions.multiple_enables.drivers.lint]
prepare_script = "mkdir ${linter} && echo dir %2 > ${linter}/ls.cmd || echo dir %2 > ${linter}/ls.cmd"
script = "ls -l ${target}"
success_codes = [0]
output = "pass_fail"

[[plugin]]
name = "multiple_enables"
version = "1.0.0"

[[plugin]]
name = "multiple_enables"
version = "1.0.0"
mode = "disabled"

[[plugin]]
name = "multiple_enables"
version = "1.0.0"
prefix = "prefix"

[[plugin]]
name = "multiple_enables"
version = "1.0.0"
mode = "disabled"
prefix = "prefix"

[[plugin]]
name = "multiple_enables"
version = "1.0.0"
prefix = "different_prefix"
