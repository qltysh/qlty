// Scala 3 indented-style snippet. Grammar coverage is best-effort.
class Scala3Sample:
  def describe(n: Int): String =
    if n > 0 then "positive"
    else if n < 0 then "negative"
    else "zero"
